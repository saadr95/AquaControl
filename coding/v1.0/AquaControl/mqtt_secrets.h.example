#pragma once

// Copy this file to mqtt_secrets.h (gitignored, never commit the real file)
// and fill in your private broker's credentials — HiveMQ Cloud Console →
// Access Management → your credential set.
#define MQTT_USERNAME "your-broker-username"
#define MQTT_PASSWORD "your-broker-password"
